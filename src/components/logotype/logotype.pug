mixin logotype(params)
  -
    params = params ? params : {};
    const {
      partial = false,
    } = params;
    const class_partial = partial ? 'logotype_type_partial' : '';

  .logotype(class=class_partial)
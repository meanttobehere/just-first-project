mixin toggle(params)
  -
    const {
      title,
      checked = false,
      name = 'name,'
    } = params;

  .toggle
    label.toggle__label 
      input.toggle__input(type='checkbox', checked=checked, name=name)
      .toggle__slider
      .toggle__border
    .toggle__title= title
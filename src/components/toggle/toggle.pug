mixin toggle(params)
  -
    const {
      title,
      checked = false,
      name = 'name,'
    } = params;

  .toggle
    label.toggle__label 
      input(type='checkbox', checked=checked, name=name).toggle__input
      .toggle__slider
      .toggle__border
    .toggle__title=title